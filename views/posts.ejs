<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="styles/button.css">
    <link rel="stylesheet" href="styles/app.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <%- include("partials/header.ejs") %>
    <div class="posts-container bg-light">

      <%if ( locals.array.length != 0 ) { %>
        <% for( let index = locals.index ; index >= 0 ; index-- ) { %>
          <div class="row d-flex justify-content-center">
            <div class="col-md-4 mt-4">
              <div class="card mb-4 box-shadow">
                <div class="card-body">
                  <div class="d-flex">
                    <p class="card-text"><span style="font-weight: bold;"><%= locals.array[index].userName %></span></p>
                    <small class="text-muted" style="margin-left: auto;"><%= locals.date %></small>  
                  </div>
                  <p class="card-text"><span style="font-weight: bold;"><%= locals.array[index].title %></span></p>
                  <p class="text-muted"><%= locals.array[index].oneline %></p>
                  <div class="d-flex justify-content-center align-items-center">
                    <small class="text-muted"><%= locals.array[index].min %> Min Reading</small>  
                    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
                      <a href="/details?index=<%= index %>"><button type="button" class="btn btn-dark btn-two mx-3" style="width: 125px; height: 40px;">Read</button></a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% } %>
      <% } else { %>
        <div class="px-4 pt-5 my-5 text-center border-bottom py-5">
          <h1 class="display-4 fw-bold text-body-emphasis my-5">There is no posts yet &#128546;</h1>
          <div class="overflow-hidden" style="max-height: 30vh;">
            <div class="container px-5">
              <img src="images/posts/sad.jpg" class="d-block mx-lg-auto img-fluid home-image" width="700" height="500">
            </div>
          </div>
        </div>
      <% } %>
    </div>

    <hr class="hr-style">
    
</body>
<%- include("partials/footer.ejs") %>
</html>